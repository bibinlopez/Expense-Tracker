### To Start the Application

1 -
Add Server Base-url in .env
VITE_BASE_API_URL="http://localhost:4000"

    Note:- The express server run on default port is 4000, and host is localhost

2 - Install packages,
command to install : npm install

2 - Start App, after the server run successfully
command to run:- npm run dev
